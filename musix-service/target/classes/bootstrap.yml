spring:
  cloud:
    config:
      uri: http://localhost:8085

server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8099/eureka/
      register-with-eureka: true
      fetch-registry: true
